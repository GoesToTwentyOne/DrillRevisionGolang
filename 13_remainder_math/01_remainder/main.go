package main

import "fmt"

func main() {
	// In mathematics, the remainder is the amount "left over" after performing some computation. In arithmetic,
	// the remainder is the integer "left over"
	// after dividing one integer by another to produce an integer quotient (integer division)
	x := 13 % 2
	fmt.Println(x)

}
